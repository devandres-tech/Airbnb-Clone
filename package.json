{
  "private": true,
  "scripts": {
    "build:server": "lerna run build --scope={@abb/common,@abb/server}"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/rimraf",
      "**/rimraf/**"
    ]
  },
  "devDependencies": {
    "lerna": "^3.17.0"
  },
  "name": "Aribnb-Clone"
}
